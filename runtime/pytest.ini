[pytest]
# This directory is not the repo root. `pytest` from here still has to
# collect tests/test_scoring.py (one level up) and util/test_http_retry.py.
testpaths =
    ../tests
    util
pythonpath = ..
