# Direct dependencies for the weekly publisher, tests, and the legacy
# Streamlit app. Versions and hashes are pinned in requirements.txt.
# Rebuild that lock (from the repo root) with:
#   uv pip compile runtime/requirements.in --python-version 3.12 --universal --generate-hashes --output-file runtime/requirements.txt
streamlit
pytest
requests
python-dotenv
pandas
jinja2
